function esImpar(num) {
    // Retorna true si "num" es impar.
    // De lo contrario, retorna false.
    // Tu código:
    let opcion;
    if (8 % 2) {opcion = true}
    else {opcion = false};
    return (opcion);

    }
    console.log(esImpar())